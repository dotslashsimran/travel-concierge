# Install with pip (not uv):
#   pip install -r requirements.txt
#
# crewai pins opentelemetry-api<1.35 but neatlogs pulls in
# opentelemetry-instrumentation-openai which declares >=1.38. uv refuses
# this as unsatisfiable. pip installs both and it works in practice because
# the actual code paths are compatible with 1.34.x.

neatlogs[crewai,google-genai]==1.3.1
# -e ../neatlogs[crewai,google-genai]
crewai==1.14.1
openinference-instrumentation-crewai==1.1.2
google-genai
httpx
python-dotenv
rich
